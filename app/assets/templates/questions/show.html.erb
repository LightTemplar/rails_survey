<ng-form class="card" name="questionEditorForm" ng-controller="ShowQuestionCtrl">
  <div class="card-body card-padding">
    <div class="row m-t-5">
      <label class="col-md-3">Identifier</label>
      <div class="col-md-9">
        <input type="text"
        class="form-control"
        name="question-identifier"
        ng-model="question.question_identifier"
        required>
      </div>
    </div>

    <div class="row m-t-5">
      <label class="col-md-3">Type</label>
      <div class="col-md-9">
        <select
        data-placeholder="Select a question type..."
        no-results-text="'This does not match any valid question type'"
        name="question-type"
        id="question-type"
        ng-model="question.question_type"
        ng-options="type for type in questionTypes()"
        required>
          <option value=""></option>
        </select>
      </div>
    </div>

    <div class="row m-t-5">
      <label class="col-md-3">Text</label>
      <div class="col-md-9">
        <textarea
        class="form-control"
        name="question-text"
        ng-model="question.text"
        ng-required="true">
        </textarea>
      </div>
    </div>

    <div class="row m-t-5"
    ng-show="question.question_type && questionTypesWithOptions(question.question_type)">
      <label class="col-md-3">Option Set</label>
      <div class="col-md-9">
        <select
          name="option-set"
          id="option-set"
          ng-model="question.option_set_id"
          ng-options="set.id as set.title for set in optionSets">
          <option value=""></option>
        </select>
      </div>
    </div>

    <div class="row m-t-5">
      <div class="col-md-4">
        <div class="btn btn-default" ng-click="cancel()">Cancel</div>
      </div>
      <div class="col-md-4" ng-show="question.id">
        <div class="btn btn-danger" ng-click="deleteQuestion()">Delete</div>
      </div>
      <div class="col-md-4">
        <div class="btn btn-success" ng-disabled="questionEditorForm.$invalid"
        ng-click="saveQuestion()">Save</div>
      </div>
    </div>
  </div>
</ng-form>
