<h4 class="text-center"><strong>Translations for {{language}}</strong></h4>
<div class="card">
  <div class="card-body card-padding">
    <div ng-controller="InstructionTranslationsCtrl">
      <table class="table table-hover">
        <thead>
          <tr class="row">
            <th class="col-md-6">Instruction Text</th>
            <th class="col-md-6">Translation</th>
          </tr>
        </thead>
        <tbody>
          <tr class="row" ng-repeat="instruction in instructions">
            <td class="col-md-6">{{instruction.text}}</td>
            <td class="col-md-6">
              <textarea
              class="form-control"
              name="instruction-translation-{{instruction.id}}"
              ng-model="translationFor(instruction).text">
            </textarea>
            </td>
          </tr>
        </tbody>
      </table>
      <div class="m-1">
        <div class="btn btn-success pull-right" ng-click="save()">Save</div>
      </div>
    </div>
  </div>
</div>
