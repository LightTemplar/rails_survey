<div class="card" ng-controller="DisplayTranslationsCtrl">
  <div class="card-body card-padding">
    <h4 class="text-center">Translations</h4>
    <div class="row m-1">
      <label class="col-sm-4 text-right">Language</label>
      <div class="col-sm-8 form-group text-left">
        <select
          class="form-control"
          ng-model="language"
          ng-options="language[1] as language[0] for language in languages">
        </select>
      </div>
    </div>
    <div ng-show="language">
      <h5 class="row m-1">
        <div class="col-md">Display Title</div>
        <div class="col-md">Translation</div>
      </h5>
      <div class="row m-1" ng-repeat="display in displays">
        <div class="col-md">{{display.title}}</div>
        <div class="col-md">
          <textarea
            class="form-control"
            name="display-translation-{{display.id}}"
            ng-model="translationFor(display).text">
          </textarea>
        </div>
      </div>
      <div class="btn btn-success float-right m-1" ng-click="saveTranslations()">Save</div>
    </div>
  </div>
</div>
